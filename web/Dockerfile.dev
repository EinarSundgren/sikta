# Development Dockerfile for React with Vite
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy source (will be overwritten by volume mount)
COPY . .

# Expose Vite dev server port
EXPOSE 3000

# Run Vite dev server
# --host exposes to all interfaces (needed for container)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]
